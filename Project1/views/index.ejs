<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-800 mt-2 w-full rounded-md outline-none" type="text" placeholder="title goes here..." name="title">
                <textarea class="block px-5 py-2 bg-zinc-800 mt-2 w-full rounded-md resize-none outline-none" placeholder="write your task detail" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="create task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <%
                if(files.length>0){%>
                    <%
                        files.forEach((val)=>{%>
                            <div class="w-72 bg-zinc-800 px-3 py-4 rounded-md">
                                <h1 class="text-3xl text-white tracking-tight"><%= val%></h1>
                                <div class="w-full flex justify-between mt-2">
                                    <a class="text-blue-500" href="/file/<%= val%>">Read More.</a>
                                    <a class="text-zinc-500" href="/edit/<%= val%>">Edit fileName</a>
                                </div>
                            </div>
                        <%})            
                    %>
                <%}else{%>
                    <h1 class="text-zinc-600">No tasks yet</h1>
                <%}
            %>
            
            
        </div>
    </div>
</body>
</html>